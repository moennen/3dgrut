# @package _global_
# NHT (Neural Harmonic Textures) variant for colmap datasets with 3DGRT and MCMC

defaults:
  - /base_mcmc
  - /dataset: colmap
  - /initialization: colmap
  - /render: 3dgrt
  - _self_

model:
  feature_type: "nht"
